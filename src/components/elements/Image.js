import React from 'react';
import { Image as DefaultImage } from 'react-native';

const Image = (props) => {


    return (
        <DefaultImage
            {...props}
        />
    );
};

export default Image;
